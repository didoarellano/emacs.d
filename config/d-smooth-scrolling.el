(use-package smooth-scrolling
  :ensure t
  :init (setq smooth-scrolling-margin 5))

;; (use-package sublimity
;;   :ensure t
;;   :config
;;   (require 'sublimity-scroll)
;;   ;; TODO Try toggling on only when jumping around during a search (isearch,
;;   ;; evil-search, etc.). It's nauseating otherwise.
;;   ;; (sublimity-mode 1)
;;   )

(provide 'd-smooth-scrolling)
